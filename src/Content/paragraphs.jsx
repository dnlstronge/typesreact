import React from "react";

const sectionBpara = {
    p1: `Built this little section to practice writing unit tests for a few simple components, also 
    as I am now using Typescript I wanted to make sure I could get jest set up to work using ts-jest. The compnents
    are simple and so are the tests. The increment counter I wanted to check that the dom element would display
    the expected corresponding value e.g plus or minus`,
}

export { sectionBpara }